# Pinned to the versions the mission ledger records, plus what the frozen artifacts were produced
# under. pysubgroup 0.9.0 is load-bearing: the search's beam and quality functions are its own.
numpy==1.26.4
pandas
pyarrow
scikit-learn
scipy
pysubgroup==0.9.0
chess
zstandard
